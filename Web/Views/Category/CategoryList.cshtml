@using Entities
@model IEnumerable<Category>
@{
    ViewData["Title"] = "CategoryList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="/css/AdminTable.css" rel="stylesheet" />
@if (Model.Any())
{
    <table class="table table-striped table-bordered text-center">
        <thead class="thead-dark">
            <tr>
                <th class="text-white">
                    <a href="/Category/SortByIdDescending">
                        ID
                    </a>
                </th>
                <th class="text-white">
                    <a href="/Category/SortByNameDescending">
                        Kategori
                    </a>
                </th>
                <th class="text-white">
                    <a href="/Category/SortByDateDescending">
                        Oluşturulma Tarihi
                    </a>
                </th>
                <th class="text-white">
                    <a href="/Category/SortByStatusDescending">
                        Statü
                    </a>
                </th>
                <th>Sil</th>
                <th>Güncelle</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in Model)
            {
                <tr>
                    <td>@category.Id</td>
                    <td>@category.Name</td>
                    <td>@category.CreateDate</td>
                    <td>
                        @if (category.Status == true)
                        {
                            <a id="statu" style="padding:2px 12px;border-radius:80%" class="@category.Status btn-success"></a>
                        }
                        else
                        {
                            <a id="statu" style="padding:2px 12px;border-radius:80%" class="@category.Status btn-danger"></a>
                        }
                    </td>
                    <td>
                        <a onclick="confirmDelete(@category.Id)" class="btn btn-danger text-white">
                            <i class="fa fa-trash"></i>
                        </a>
                    </td>
                    <td>
                        <a href="/Category/CategoryUpdate/@category.Id/" class="btn btn-primary">
                            <i class="fa fa-refresh"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @if (ViewBag.PageSize > 0 && ViewBag.Count > ViewBag.PageSize)
    {
        <div class="text-center">
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center">
                    @if (ViewBag.PageNumber > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" href="/Category/CategoryList?page=1&pageSize=@ViewBag.PageSize">
                                <span aria-hidden="true">İlk</span>
                            </a>
                        </li>
                        <li class="page-item">
                            <a class="page-link" href="/Category/CategoryList?page=@(ViewBag.PageNumber - 1)&pageSize=@ViewBag.PageSize">
                                <span aria-hidden="true">&lsaquo;</span>
                            </a>
                        </li>
                    }

                    @for (int i = 1; i <= Math.Ceiling((decimal)ViewBag.Count / ViewBag.PageSize); i++)
                    {
                        <li class="page-item @(i == ViewBag.PageNumber ? "active" : "")">
                            <a class="page-link" href="/Category/CategoryList?page=@i&pageSize=@ViewBag.PageSize">@i</a>
                        </li>
                    }

                    @if (ViewBag.PageNumber < Math.Ceiling((decimal)ViewBag.Count / ViewBag.PageSize))
                    {
                        <li class="page-item">
                            <a class="page-link" href="/Category/CategoryList?page=@(ViewBag.PageNumber + 1)&pageSize=@ViewBag.PageSize">
                                <span aria-hidden="true">&rsaquo;</span>
                            </a>
                        </li>
                        <li class="page-item">
                            <a class="page-link" href="/Category/CategoryList?page=@(Math.Ceiling((decimal)ViewBag.Count / ViewBag.PageSize))&pageSize=@ViewBag.PageSize">
                                <span aria-hidden="true">Son</span>
                            </a>
                        </li>
                    }
                </ul>
            </nav>
        </div>
    }
    <div class="col-12 text-center">
        <h6>
            <div class="row">
                <div class="col-sm-12 text-center"><span id="Count">@ViewBag.Count</span> Adet Kategori Bulunmaktakdır.</div>
                <div class="col-sm-12 text-center">
                    <span id="True">@ViewBag.True</span> tanesi Aktif,
                    <span>
                        @if (@ViewBag.False > 0)
                        {
                            <span id="False">
                                @ViewBag.False
                            </span>
                        }
                        else
                        {
                            <span id="Zero">
                                @ViewBag.False
                            </span>
                        }
                    </span> tanesi Pasif.
                </div>
            </div>
        </h6>
    </div>

}
else
{
    <center>
        <div class="alert alert-danger text-center mt-4" style="width:70%;">
            <div class="mt-1 mb-1">
                <b>Kategori Bulunamadı.</b>
            </div>
        </div>
    </center>
}
<div class="text-center">
    @if (!string.IsNullOrEmpty(TempData["ErrorMessage"] as string))
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }
</div>
@await Html.PartialAsync("~/Views/Category/CategoryAdd.cshtml")

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

<script>
    function confirmDelete(id) {
        Swal.fire({
            title: 'Emin misiniz?',
            text: "Bu öğeyi silmek istediğinizden emin misiniz?",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonText: 'Evet, Sil',
            cancelButtonText: 'Hayır, Vazgeç',
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = '/Category/CategoryDelete/' + id;
            }
        });
    }
    var successMessage = "@TempData["SuccessMessage"]";
    if (successMessage) {
        Swal.fire('Başarılı', successMessage, 'success');
        setTimeout(function () {
        }, 3000);
    }
</script>

